# This root-level .gitignore applies to all subdirectories of this repo
# so it should only be used for things that are truly universal.

# Binaries for programs and plugins
*.exe
*.exe~
*.dll
*.so
*.dylib

# Test binary, built with `go test -c`
*.test

# Output of the go coverage tool, specifically when used with LiteIDE
*.out

# VIM temp files
*.swp
